import { createStyles } from '@mantine/core';

export default createStyles((theme) => ({
  container: {
    background: `linear-gradient(to bottom, ${theme.colors['raga-green'][1]}, ${theme.colors['raga-green'][2]})`

  },
}));
